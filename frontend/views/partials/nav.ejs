    <nav class="navbar navbar-expand-xl navbar-dark fixed-top shadow-sm transition-all">
        <div class="container">
            <a class="navbar-brand fw-bold d-flex align-items-center gap-2 mb-0" href="<%= site.routes.home %>">
                <img src="<%= design.images.logo || site.icon_path %>" alt="<%= site.legal_name %> logo" class="navbar-logo-mark">
                <span class="brand-full"><%= site.legal_name %></span>
                <span class="badge-live-public d-none d-md-inline-flex"><i class="fas fa-circle"></i>Live</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-xl-auto align-items-xl-center">
                    <li class="nav-item"><a class="nav-link nav-link-home <%= activePage === 'home' ? 'active' : '' %>" href="<%= site.routes.home %>">Home</a></li>
                    <li class="nav-item"><a class="nav-link nav-link-services <%= activePage === 'services' ? 'active' : '' %>" href="<%= site.routes.services %>">Services</a></li>
                    <li class="nav-item"><a class="nav-link nav-link-store <%= activePage === 'store' ? 'active' : '' %>" href="/store">Store</a></li>
                    <li class="nav-item"><a class="nav-link nav-link-contact <%= activePage === 'contact' ? 'active' : '' %>" href="<%= site.routes.contact %>">Contact</a></li>
                    <li class="nav-item"><a class="nav-link nav-link-about <%= activePage === 'about' ? 'active' : '' %>" href="<%= site.routes.about %>">About</a></li>
                    <li class="nav-item d-flex align-items-center">
                        <%- include('language-switcher') %>
                    </li>
                    <% if (typeof user !== 'undefined' && user) { %>
                        <li class="nav-item"><a class="nav-link <%= activePage === 'account' ? 'active' : '' %>" href="/account"><i class="fas fa-user-circle me-1"></i><%= user.name %></a></li>
                    <% } %>
                    <li class="nav-item"><a class="nav-link position-relative <%= activePage === 'cart' ? 'active' : '' %>" href="/cart" title="Cart">
                        <i class="fas fa-cart-shopping"></i><span class="cart-badge d-none" data-cart-count></span>
                    </a></li>
                    <% if (typeof user !== 'undefined' && user) { %>
                        <li class="nav-item"><a class="nav-link nav-link-cta" href="/logout"><i class="fas fa-arrow-right-from-bracket me-1"></i>Logout</a></li>
                    <% } else { %>
                        <li class="nav-item"><a class="nav-link nav-link-cta nav-link-login" href="#" data-open-login><i class="fas fa-right-to-bracket me-1"></i>Login / Sign Up</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    <main>
